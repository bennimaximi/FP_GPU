[project]
name = "fp-gpu"
version = "0.1.0"
description = "FP-Praktikum"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
